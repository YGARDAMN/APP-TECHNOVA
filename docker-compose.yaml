version: '3'
services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "8080:8080"
    depends_on:
      - sonarqube
    networks:
      - devops-net

  sonarqube:
    image: sonarqube:lts
    ports:
      - "9000:9000"
    networks:
      - devops-net

networks:
  devops-net:
